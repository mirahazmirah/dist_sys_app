apiVersion: apps/v1
kind: Deployment
metadata:
  name: campus-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: campus-postgres
  template:
    metadata:
      labels:
        app: campus-postgres
    spec:
      containers:
      - name: postgres
        image: postgres:15-alpine
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_USER
          value: "postgres"
        - name: POSTGRES_PASSWORD
          value: "campus123"
        - name: POSTGRES_DB
          value: "campusride"
---
apiVersion: v1
kind: Service
metadata:
  name: campus-postgres-service
spec:
  selector:
    app: campus-postgres
  ports:
  - port: 5432
    targetPort: 5432
  type: ClusterIP