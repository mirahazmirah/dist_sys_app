config:
  target: "http://127.0.0.1:8000"
  phases:
    - duration: 60
      arrivalRate: 10
      name: "Warm up"
    - duration: 120
      arrivalRate: 50
      name: "Peak load"

scenarios:
  - name: "Get students"
    flow:
      - get:
          url: "/api/v1/students/"